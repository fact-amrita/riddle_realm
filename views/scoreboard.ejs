<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="scoreboard.css">
</head>

<body>
    <main>
        <div id="header">
            <h1>Ranking</h1>
        </div>
        <div id="leaderboard">
            <div class="ribbon"></div>
            <table>
                <% for( let i=0; i < scores.length; i++ ) { %>
                    <tr>
                        <td>
                            <%= i+1 %>
                        </td>
                        <td>
                            <%= scores[i].teamMember1 %><br>
                                <%= scores[i].teamMember2 %>
                        </td>
                        <td>
                            <a style="color:<% if (i==0) { %><%="white"%><% } else { %><%="black"%> <% } %>" href="/points/<%= scores[i].TeamID %>"><%= scores[i].TeamID %>
                            </a>
                        </td>
                        <td>
                            <%= scores[i].TeamScore %>
                        </td>
                    </tr>
                    <% } %>
            </table>
            <div id="buttons">
                <button class="continue" onclick="window.location.href='/team'">Access a team</button>
            </div>
        </div>
    </main>
</body>

</html>